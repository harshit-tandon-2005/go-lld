# Single Responsibility Principle

A single class or module should have only one reason to change i.e. it should have only one responsibility or job.

Note- A single struct shouldn't have value receiver methods which give different responsibilitess (refer examples)